apiVersion: v1
kind: Service
metadata:
  name: ms-orizon-api-nodeport
  labels:
    app: ms-orizon-api
    service-type: nodeport
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: api
  selector:
    app: ms-orizon-api
